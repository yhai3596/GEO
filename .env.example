# 🔧 GEO智能评估平台 - 环境变量配置模板

# ==========================================
# 🗄️ 数据库配置 (必需)
# ==========================================

# Supabase数据库连接URL
# 格式: postgresql://postgres:[password]@[host]:5432/postgres
DATABASE_URL=postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres

# ==========================================
# 🔑 Supabase配置 (必需)
# ==========================================

# Supabase项目URL
# 示例: https://your-project.supabase.co
SUPABASE_URL=https://your-project.supabase.co

# Supabase匿名密钥 (用于前端)
# 从Supabase项目设置 > API > anon public 获取
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key

# Supabase服务角色密钥 (用于后端管理操作)
# 从Supabase项目设置 > API > service_role secret 获取
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-service-role-key

# ==========================================
# 🔐 JWT配置 (必需)
# ==========================================

# JWT签名密钥 (生产环境请使用强密码)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT过期时间 (可选，默认24小时)
JWT_EXPIRES_IN=24h

# ==========================================
# 🌐 服务器配置 (可选)
# ==========================================

# 应用运行端口 (本地开发用)
PORT=3001

# 运行环境
NODE_ENV=development

# 前端URL (CORS配置用)
CLIENT_URL=http://localhost:5173

# ==========================================
# 📧 邮件服务配置 (可选)
# ==========================================

# SMTP服务器配置
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# 邮件账号信息
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# 发件人信息
EMAIL_FROM=GEO智能评估平台 <noreply@geo-platform.com>

# ==========================================
# 🔧 外部服务配置 (可选)
# ==========================================

# Redis缓存 (如果使用)
REDIS_URL=redis://localhost:6379

# 文件存储 (如果使用云存储)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-s3-bucket

# ==========================================
# 📊 监控和日志 (可选)
# ==========================================

# 应用监控
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# 日志级别
LOG_LEVEL=info

# ==========================================
# 🚀 Vercel部署专用配置
# ==========================================

# Vercel部署时，以下变量会自动设置：
# VERCEL=1
# VERCEL_ENV=production
# VERCEL_URL=your-app.vercel.app

# ==========================================
# 📝 配置说明
# ==========================================

# 1. 复制此文件为 .env
# 2. 填写所有必需的环境变量
# 3. 确保 .env 文件已添加到 .gitignore
# 4. 生产环境请使用强密码和安全的密钥

# ⚠️ 安全提醒：
# - 不要将包含真实密钥的 .env 文件提交到版本控制
# - 生产环境请使用强密码
# - 定期更换密钥和密码
# - 限制数据库和API的访问权限